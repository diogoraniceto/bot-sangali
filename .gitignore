# bot_sangali

# Bot Control Panel (node_modules excluded)
bot-control-panel/node_modules/

# Environment variables
.env

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
venv/
.venv/
reembed_total.log
tests/results_*.csv

# Backups de vetores/embeddings (dezenas de MB, repo publico). O .sql de backup da RPC continua rastreado.
migrations/backup/*.json
